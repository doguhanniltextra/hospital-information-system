apiVersion: external-secrets.io/v1
kind: SecretStore
metadata:
  name: billing-service-vault-store
  labels:
    app.kubernetes.io/name: billing-service
    app.kubernetes.io/instance: billing-service
    app.kubernetes.io/component: backend
    app.kubernetes.io/part-of: hospital-information-system
    app.kubernetes.io/managed-by: kustomize
spec:
  provider:
    vault:
      server: "http://vault.vault.svc.cluster.local:8200"
      path: "secret"
      version: "v2"
      auth:
        kubernetes:
          mountPath: "kubernetes"
          role: "billing-service-role"
          serviceAccountRef:
            name: billing-service-sa
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: billing-service-vault-secret
  labels:
    app.kubernetes.io/name: billing-service
    app.kubernetes.io/instance: billing-service
    app.kubernetes.io/component: backend
    app.kubernetes.io/part-of: hospital-information-system
    app.kubernetes.io/managed-by: kustomize
spec:
  refreshInterval: "1h"
  secretStoreRef:
    name: billing-service-vault-store
    kind: SecretStore
  target:
    name: billing-service-secret
    creationPolicy: Owner
  data:
    - secretKey: SPRING_DATASOURCE_WRITE_USERNAME
      remoteRef:
        key: hospital/billing-service/db
        property: username
    - secretKey: SPRING_DATASOURCE_WRITE_PASSWORD
      remoteRef:
        key: hospital/billing-service/db
        property: password
    - secretKey: SPRING_DATASOURCE_READ_USERNAME
      remoteRef:
        key: hospital/billing-service/db
        property: username
    - secretKey: SPRING_DATASOURCE_READ_PASSWORD
      remoteRef:
        key: hospital/billing-service/db
        property: password
    - secretKey: INVOICE_API_KEY
      remoteRef:
        key: hospital/billing-service/api-keys
        property: invoice_api_key
    - secretKey: APP_SECRET
      remoteRef:
        key: hospital/shared/jwt
        property: app_secret
    - secretKey: INTERNAL_SERVICE_TOKEN
      remoteRef:
        key: hospital/shared/internal-auth
        property: internal_token
