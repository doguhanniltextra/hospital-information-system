apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: his-ingress
spec:
  rules:
    - host: his.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: api-gateway
                port:
                  number: 4004
